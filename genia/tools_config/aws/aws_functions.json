[
    {
        "name": "list_aws_regions",
        "description": "Use the AWS API to list all AWS regions",
        "parameters": {
            "type": "object",
            "properties": {
            },
            "required": [
            ]
        }
    }
]